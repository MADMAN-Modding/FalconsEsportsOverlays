const invoke = window.__TAURI__.core.invoke;

// Turns off context menu on release
// if (window.location.hostname !== 'tauri.localhost') {
//     document.addEventListener('contextmenu', event => event.preventDefault());
// }